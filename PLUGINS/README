While we can actually store the Plugin files here, it will be easier to just do the same with plugins as we do the mods. Each plugin will have a txt file with it's version and the link to the it's page on the ORE.Sponge repository.
