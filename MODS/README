Due to size restrictions, we cannot store the actual mod files here. What we can do however is create a txt file entry for every mod that lists it's version # and link to it's download page. Anytime we update the modpack via SOLDER, the files that were updated need to be updated here as well. 
